import { Meta, ColorPalette, ColorItem } from '@storybook/addon-docs/blocks';
import { useTheme, ThemeProvider } from 'styled-components';
import { theme } from '@theme';

<Meta title="Colors" />

# Colors

#

#

#

export const ColorPaletteComponent = () => {
    const theme = useTheme();

    return (
        <>
            <h2>Brand</h2>
            <ColorPalette>
                <ColorItem
                    title="Brand Colors"
                    colors={{
                        C1: theme.colors.brand.c1,
                        C2: theme.colors.brand.c2,
                        C3: theme.colors.brand.c3,
                        C4: theme.colors.brand.c4,
                        C5: theme.colors.brand.c5
                    }}
                />
            </ColorPalette>

            <h2>Global</h2>
            <ColorPalette>
                <ColorItem
                    title="Black & White"
                    
                    colors={{
                        Black: theme.colors.global.black,
                        White: theme.colors.global.white
                    }}
                />
            </ColorPalette>

            

            <h2>Feedback</h2>
            <ColorPalette>
                <ColorItem
                    title="Feedback Colors"
                    colors={{
                        Positive: theme.colors.feedback.positive,
                        Negative: theme.colors.feedback.negative,
                        Warning: theme.colors.feedback.warning,
                    }}
                />
            </ColorPalette>
        </>
    );
};

<ThemeProvider theme={theme}>
    <ColorPaletteComponent />
</ThemeProvider>

